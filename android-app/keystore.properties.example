# Copie para keystore.properties na raiz de android-app/ (não versionar).
# Gere o keystore: keytool -genkey -v -keystore nexshape-release.jks -keyalg RSA -keysize 2048 -validity 10000 -alias nexshape

storeFile=nexshape-release.jks
storePassword=ALTERAR
keyAlias=nexshape
keyPassword=ALTERAR
